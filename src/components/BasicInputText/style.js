import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
    basicInputText: {
        borderWidth:1, 
        padding: 3, 
        marginBottom: 10,
        borderColor: 'azure',
        color: 'azure',
        fontSize: 18
    }
});

export default style;