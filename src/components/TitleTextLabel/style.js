import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
    titleLabel: {
        fontSize: 15, 
        marginTop: 5, 
        marginBottom: 10, 
        color: 'azure', 
        fontWeight: "bold"
    }
});

export default style;